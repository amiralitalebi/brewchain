# Smart Contract

Very small Algorand smart contract for batch proof support.

## Planned responsibility

- create_batch(batch_id)
- add_event(batch_id, stage, event_hash)
- batch_exists(batch_id)
- get_event_count(batch_id)
